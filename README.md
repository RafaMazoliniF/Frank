# VM-Compiladores

Comandos para gerar o exe

```bash
cd backend
python -m PyInstaller --onefile app.py
```
Mover o app.exe da pasta dist para dentro do backend apenas

```bash
cd electron
npm install
npm run build
```
